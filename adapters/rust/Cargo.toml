[package]
name = "prauga-flexdoc-axum"
version = "0.5.6"
edition = "2021"
license = "AGPL-3.0-or-later"
description = "Self-contained Axum adapter for the Prauga FlexDoc OpenAPI renderer"
repository = "https://github.com/prauga/flexdoc"
homepage = "https://github.com/prauga/flexdoc"
readme = "README.md"
keywords = ["openapi", "axum", "api-docs", "self-hosted", "api-explorer"]
categories = ["web-programming", "development-tools"]
include = ["src/**", "assets/**", "README.md", "Cargo.toml"]

[features]
default = ["host-execution"]
host-execution = ["dep:prauga-flexdoc-host-execution", "dep:futures-util", "dep:multer"]

[dependencies]
prauga-flexdoc-host-execution = { version = "0.1.1", path = "../rust-host-execution", default-features = false, features = ["host-execution"], optional = true }
axum = "0.8"
futures-util = { version = "0.3", optional = true }
multer = { version = "3", optional = true }
serde = "1"
serde_json = "1"

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt-multi-thread", "net", "time"] }
tower = { version = "0.5", features = ["util"] }

[[test]]
name = "host_execution"
path = "tests/host_execution.rs"
required-features = ["host-execution"]

[[test]]
name = "host_execution_security"
path = "tests/host_execution_security.rs"
required-features = ["host-execution"]
