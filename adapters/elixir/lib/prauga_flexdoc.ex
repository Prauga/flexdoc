defmodule PraugaFlexDoc do
  @moduledoc """
  Self-hosted FlexDoc integration for Plug and Phoenix applications.

  The package exposes `PraugaFlexDoc.Config` and `PraugaFlexDoc.Plug`, a
  self-contained Plug that serves the docs shell and packaged renderer assets.
  """
end
