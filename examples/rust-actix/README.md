# Actix Web + FlexDoc

This example uses published `prauga-flexdoc-actix` `0.4.2` from crates.io. Repository CI patches the crate to the local `adapters/rust-actix` source when validating adapter changes.
