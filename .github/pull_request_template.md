## Summary

## Test plan

- [ ] Tests covering the change pass
- [ ] Client changes include a rebuilt standalone renderer and synchronized adapter assets
- [ ] No secrets, tokens, or cookie values are added to docs, tests, or commit messages

Commits include a `Signed-off-by` line (`git commit -s`).
